#include "ControlaTiro.h"

void ControlaTiro::atirou(int x, int y, int vel, std::string quemAtirou)
{
	/*Tiro t;
	t.setX(x);
	t.setY(y);
	t.setVelocidade(vel);
	t.setaQuemChamou(quemAtirou);

	this->tiros.push_back(t);*/
}

void ControlaTiro::moveTiros()
{
	/*std::list<Tiro>::iterator it;
	for (it = this->tiros.begin(); it != this->tiros.end(); ++it) {
		if (it->estaVivo()) {
			it->moveTiro();
		}
	}*/
}
